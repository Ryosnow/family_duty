{
  "name": "family-duty-mini-program",
  "version": "1.0.0",
  "private": true,
  "description": "家庭值日离线微信小程序",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/node": "latest",
    "miniprogram-api-typings": "latest",
    "typescript": "latest",
    "vitest": "latest"
  }
}
